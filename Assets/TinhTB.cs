using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TinhTB : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int A = 5;
        int B = 6;
        int C = 7;
        int D = 8;

        Debug.Log((A + B + C + D) / 4);
        


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
